"use strict";

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var DBHelper = function () {
  function DBHelper() {
    _classCallCheck(this, DBHelper);
  }

  _createClass(DBHelper, null, [{
    key: "openDatabase",
    value: function openDatabase() {
      var reviewsList;var dbPromise = idb.open("restaurants", 2, function (upgradeDb) {
        switch (upgradeDb.oldVersion) {case 0:
            if (!upgradeDb.objectStoreNames.contains("restaurantsList")) {
              upgradeDb.createObjectStore("restaurantsList", { keyPath: "id" });
            }
          case 1:
            if (!upgradeDb.objectStoreNames.contains("reviewsList")) {
              var reviewsStore = upgradeDb.createObjectStore("reviewsList", { keyPath: "id" });reviewsStore.createIndex('restaurant_id', 'restaurant_id');
            }}
      });return dbPromise;
    }
  }, {
    key: "fetchRestaurants",
    value: function fetchRestaurants(callback) {
      if (navigator.onLine) {
        var url = DBHelper.DATABASE_URL + 'restaurants';fetch(url, { method: "GET" }).then(function (resp) {
          return resp.json();
        }).then(function (restaurants) {
          var dbPromise = DBHelper.openDatabase();dbPromise.then(function (db) {
            if (!db) return;var tx = db.transaction("restaurantsList", "readwrite");var store = tx.objectStore("restaurantsList");restaurants.forEach(function (res) {
              store.put(res);
            });return tx.complete;
          });callback(null, restaurants);
        }).catch(function (error) {
          callback(error, null);
        });
      } else {
        return DBHelper.cacheRestaurantsFromDb().then(function (restaurants) {
          if (restaurants.length) {
            callback(null, restaurants);
          }
        });
      }
    }
  }, {
    key: "cacheRestaurantsFromDb",
    value: function cacheRestaurantsFromDb() {
      var dbPromise = DBHelper.openDatabase();var restaurants = dbPromise.then(function (db) {
        var tx = db.transaction("restaurantsList", "readonly");var store = tx.objectStore("restaurantsList");return store.getAll();
      });return restaurants;
    }
  }, {
    key: "cacheReviewsFromDb",
    value: function cacheReviewsFromDb(id) {
      var dbPromise = DBHelper.openDatabase();var reviews = dbPromise.then(function (db) {
        var tx = db.transaction("reviewsList", "readonly");var store = tx.objectStore("reviewsList");return store.getAll(id);
      });return reviews;
    }
  }, {
    key: "fetchReviewsByRestaurantId",
    value: function fetchReviewsByRestaurantId(id, callback) {
      if (navigator.onLine) {
        var url = DBHelper.DATABASE_URL + "reviews/?restaurant_id=" + id;fetch(url, { method: "GET" }).then(function (resp) {
          return resp.json();
        }).then(function (reviews) {
          callback(null, reviews);
        }).catch(function (error) {
          callback(error, null);
        });
      } else {
        return DBHelper.cacheReviewsFromDb().then(function (reviews) {
          if (reviews.length) {
            callback(null, reviews);
          }
        });
      }
    }
  }, {
    key: "saveRestaurantsFavoriteToDatabase",
    value: function saveRestaurantsFavoriteToDatabase(restaurants) {
      var dbPromise = DBHelper.openDatabase();dbPromise.then(function (db) {
        if (!db) return;var tx = db.transaction("restaurantsList", "readwrite");var store = tx.objectStore("restaurantsList");restaurants.forEach(function (res) {
          store.getAll(restaurants.id).put(res);
        });return tx.complete;
      });
    }
  }, {
    key: "saveReviewsToDatabase",
    value: function saveReviewsToDatabase(reviews) {
      var dbPromise = DBHelper.openDatabase();dbPromise.then(function (db) {
        if (!db) return;var tx = db.transaction("reviewsList", "readwrite");var store = tx.objectStore("reviewsList");if (reviews && (typeof reviews === "undefined" ? "undefined" : _typeof(reviews)) === 'object' && reviews.constructor === Array) {
          reviews.forEach(function (res) {
            store.put(res);
          });
        } else {
          store.put(reviews);
        }
        return tx.complete;
      });
    }
  }, {
    key: "saveRestaurantFavoriteToDatabase",
    value: function saveRestaurantFavoriteToDatabase(isFavorite, restaurantId) {
      var dbPromise = DBHelper.openDatabase();dbPromise.then(function (db) {
        if (!db) return;var tx = db.transaction("restaurantsList", "readwrite");var store = tx.objectStore("restaurantsList");store.get(restaurantId).then(function (restaurant) {
          restaurant.is_favorite = isFavorite.toString();store.put(restaurant);
        });
      });
    }
  }, {
    key: "sendRestaurantFavoriteToServer",
    value: function sendRestaurantFavoriteToServer(restaurantId, isFavorite) {
      console.log('Updating Restaurant Favorite : ', isFavorite);var url = "http://localhost:1337/restaurants/" + restaurantId + "/?is_favorite=" + isFavorite;fetch(url, { method: 'PUT' }).then(function (response) {
        return response.json();
      }).then(function (data) {
        console.log("Saved favorite successfully");DBHelper.saveRestaurantFavoriteToDatabase(isFavorite, restaurantId);
      }).catch(function (error) {
        return console.log('error:', error);
      });
    }
  }, {
    key: "sendReviewToServer",
    value: function sendReviewToServer(reviewData) {
      console.log('Sending Reviews : ', reviewData);var fetch_options = { method: 'POST', body: JSON.stringify(reviewData), headers: new Headers({ 'Content-Type': 'application/json' }) };fetch("http://localhost:1337/reviews", fetch_options).then(function (response) {
        return response.json();
      }).then(function (data) {
        console.log("Saved reviews successfully");DBHelper.saveReviewsToDatabase(data);
      }).catch(function (error) {
        return console.log('error:', error);
      });
    }
  }, {
    key: "saveReviewsTolocalStorage",
    value: function saveReviewsTolocalStorage(offlineReviewsList) {
      window.localStorage.setItem("reviews", JSON.stringify(offlineReviewsList));
    }
  }, {
    key: "saveRestaurantTolocalStorage",
    value: function saveRestaurantTolocalStorage(offlineRestaurantList) {
      window.localStorage.setItem("restaurant_favorite", JSON.stringify(offlineRestaurantList));
    }
  }, {
    key: "getRestaurantsFromlocalStorage",
    value: function getRestaurantsFromlocalStorage(id) {
      var offlineRestaurantFav = [];var offlineRestaurantFavList;if (window.localStorage.getItem("restaurant_favorite") !== null) {
        offlineRestaurantFavList = JSON.parse(window.localStorage.getItem("restaurant_favorite"));for (var i = 0; i < offlineRestaurantFavList.length; i++) {
          if (offlineRestaurantFavList[i].id === id) {
            if (window.navigator.onLine) {
              var id = offlineRestaurantFavList[i].id;var fav = offlineRestaurantFavList[i].is_favorite;DBHelper.sendRestaurantFavoriteToServer(id, fav);
            }
            offlineRestaurantFav.push(offlineRestaurantFavList[i]);offlineRestaurantFavList.splice(i, 1);
          }
          localStorage.restaurant_favorite = JSON.stringify(offlineRestaurantFavList);
        }
      }
      return offlineRestaurantFav;
    }
  }, {
    key: "getReviewsFromlocalStorage",
    value: function getReviewsFromlocalStorage(id) {
      var offlineReviews = [];var offlineReviewsList;if (window.localStorage.getItem("reviews") !== null) {
        offlineReviewsList = JSON.parse(window.localStorage.getItem("reviews"));for (var i = 0; i < offlineReviewsList.length; i++) {
          if (offlineReviewsList[i].restaurant_id === id) {
            if (window.navigator.onLine) DBHelper.sendReviewToServer(offlineReviewsList[i]);offlineReviews.push(offlineReviewsList[i]);offlineReviewsList.splice(i, 1);
          }
          localStorage.reviews = JSON.stringify(offlineReviewsList);
        }
      }
      return offlineReviews;
    }
  }, {
    key: "fetchRestaurantById",
    value: function fetchRestaurantById(id, callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var restaurant = restaurants.find(function (r) {
            return r.id == id;
          });if (restaurant) {
            callback(null, restaurant);
          } else {
            callback("Restaurant does not exist", null);
          }
        }
      });
    }
  }, {
    key: "fetchRestaurantByCuisine",
    value: function fetchRestaurantByCuisine(cuisine, callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var results = restaurants.filter(function (r) {
            return r.cuisine_type == cuisine;
          });callback(null, results);
        }
      });
    }
  }, {
    key: "fetchRestaurantByNeighborhood",
    value: function fetchRestaurantByNeighborhood(neighborhood, callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var results = restaurants.filter(function (r) {
            return r.neighborhood == neighborhood;
          });callback(null, results);
        }
      });
    }
  }, {
    key: "fetchRestaurantByCuisineAndNeighborhood",
    value: function fetchRestaurantByCuisineAndNeighborhood(cuisine, neighborhood, callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var results = restaurants;if (cuisine != "all") {
            results = results.filter(function (r) {
              return r.cuisine_type == cuisine;
            });
          }
          if (neighborhood != "all") {
            results = results.filter(function (r) {
              return r.neighborhood == neighborhood;
            });
          }
          callback(null, results);
        }
      });
    }
  }, {
    key: "fetchNeighborhoods",
    value: function fetchNeighborhoods(callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var neighborhoods = restaurants.map(function (v, i) {
            return restaurants[i].neighborhood;
          });var uniqueNeighborhoods = neighborhoods.filter(function (v, i) {
            return neighborhoods.indexOf(v) == i;
          });callback(null, uniqueNeighborhoods);
        }
      });
    }
  }, {
    key: "fetchCuisines",
    value: function fetchCuisines(callback) {
      DBHelper.fetchRestaurants(function (error, restaurants) {
        if (error) {
          callback(error, null);
        } else {
          var cuisines = restaurants.map(function (v, i) {
            return restaurants[i].cuisine_type;
          });var uniqueCuisines = cuisines.filter(function (v, i) {
            return cuisines.indexOf(v) == i;
          });callback(null, uniqueCuisines);
        }
      });
    }
  }, {
    key: "urlForRestaurant",
    value: function urlForRestaurant(restaurant) {
      return "./restaurant.html?id=" + restaurant.id;
    }
  }, {
    key: "imageUrlForRestaurant",
    value: function imageUrlForRestaurant(restaurant) {
      return "/img/" + restaurant.photograph + ".webp";
    }
  }, {
    key: "mapMarkerForRestaurant",
    value: function mapMarkerForRestaurant(restaurant) {
      var marker = new L.marker([restaurant.latlng.lat, restaurant.latlng.lng], { title: restaurant.name, alt: restaurant.name, url: DBHelper.urlForRestaurant(restaurant) });marker.addTo(newMap);return marker;
    }
  }, {
    key: "DATABASE_URL",
    get: function get() {
      var port = 1337;return "http://localhost:" + port + "/";
    }
  }]);

  return DBHelper;
}();